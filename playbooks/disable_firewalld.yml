---
- name: Disable firewalld
  hosts: homelan
  become: true
  tasks:
    - name: Stop and disable firewalld service
      ansible.builtin.service:
        name: firewalld
        state: stopped
        enabled: false
